import React, {useEffect, useState} from 'react';
import './Menu.scss'
const Menu = () => {
  return (
    <>
      <div className={'menu'}>
        <span className={'line'}/>
        <span className={'line'}/>
        <span className={'line'}/>
      </div>

    </>
  );
};

export default Menu;